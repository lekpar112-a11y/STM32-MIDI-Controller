name: Compile STM32 MIDI V8 (Force USB)

on: [push, workflow_dispatch]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v4

    - name: Setup Arduino CLI
      uses: arduino/setup-arduino-cli@v1.1.2

    # --- 1. PERBAIKAN STRUKTUR FOLDER ---
    - name: Fix Folder Structure
      run: |
        mkdir -p MidiV7
        if [ -f "MidiV7.ino" ]; then
          mv MidiV7.ino MidiV7/MidiV7.ino
        elif [ -f "main.cpp" ]; then
          mv main.cpp MidiV7/MidiV7.ino
        fi

    # --- 2. INSTALL CORE ---
    - name: Install STM32 Core (Roger Clark)
      run: |
        arduino-cli config init
        arduino-cli config add board_manager.additional_urls http://dan.drown.org/stm32duino/package_STM32duino_index.json
        arduino-cli core update-index
        arduino-cli core install stm32duino:STM32F1

    # --- 3. DEBUG: LIHAT OPSI BOARD (Jaga-jaga) ---
    # Langkah ini akan mencetak daftar menu yang valid ke Log
    # Jika gagal lagi, kita bisa lihat nama menu aslinya di sini.
    - name: Check Board Options
      run: |
        arduino-cli board details -b stm32duino:STM32F1:genericSTM32F103C

    # --- 4. INSTALL LIBRARY ---
    - name: Install Library USBComposite
      run: |
        mkdir -p libraries
        git clone https://github.com/arpruss/USBComposite_stm32f1.git libraries/USBComposite_stm32f1

    # --- 5. COMPILE DENGAN TEKNIK BYPASS ---
    - name: Compile Sketch
      run: |
        # PERUBAHAN:
        # 1. Saya menghapus bagian 'usb=composite' dari --fqbn karena itu penyebab error.
        # 2. Saya menambahkan --build-property "build.extra_flags=-DUSB_COMPOSITE"
        #    Ini fungsinya MEMAKSA fitur USB aktif tanpa lewat menu.
        
        arduino-cli compile --fqbn stm32duino:STM32F1:genericSTM32F103C:device_variant=STM32F103C8,upload_method=STLinkMethod,cpu_speed=speed_72mhz \
        --build-property "build.extra_flags=-DUSB_COMPOSITE" \
        --libraries libraries \
        --output-dir build \
        MidiV7/MidiV7.ino

    - name: Upload Firmware Artifact
      uses: actions/upload-artifact@v4
      with:
        name: stm32-midi-firmware
        path: build/
